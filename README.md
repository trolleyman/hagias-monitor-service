# `hagias` monitor service
(Hagias = friend of Hades (my desktop))

This app is meant to store monitor connfiguration, and can remotely be triggered to set this based on a command sent from another computer.
